---
title: nfs-root-escape-banner
---

{% hint style="info" %}
For both `` `--chmod` `` you need to specify the export share with `--share` if you do not want to use the root fs escape method (see [escape to root file system](../../nfs-protocol/escape-to-root-file-system.md)).
{% endhint %}

Files in NetExec are uploaded with \`777\` permissions by default. To change these permissions you can use the \`--chmod\` flag:

<figure><img src="../assets/image (25).png" alt=""><figcaption></figcaption></figure>
